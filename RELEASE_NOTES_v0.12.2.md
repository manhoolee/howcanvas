# v0.12.2 升级说明

发布日期：2026-08-04

## 解决的问题

v0.12.2 修复旧版浏览器资产迁移在部分上传失败时误删本地记录的问题，并降低大媒体目录、并发上传和批量图片任务对服务器事件循环、连接数与内存的压力。同时补齐默认模型有效性校验、前端路由按需加载和跨平台质量门禁。

## 主要变更

- 旧资产迁移仅清除服务器已确认存在或本次成功上传的记录；失败项保留并在后续登录重试。
- 用户存储配额改为首次异步统计、账号级缓存增量和并发预留，上传不再同步递归扫描全部目录。
- 图片后台任务使用 FIFO 有界并发队列，默认最多同时运行 2 项；排队取消和重启恢复走同一队列。
- 默认模型必须真实存在且能力匹配；渠道移除模型后自动清除对应失效默认值。
- 13 个页面入口改为路由级按需加载，画布项目页和各工作台形成独立构建块。
- 图片与 Seedream 任务创建、引用标签编辑器 DOM 行为和资产认证中间件完成去重。
- Agent 权限测试兼容 Windows ACL 语义，新增 Windows 与 Linux 双平台 GitHub Actions 质量门禁。

## 配置变化

- 新增可选环境变量 `IMAGE_TASK_CONCURRENCY`，默认 `2`，服务端会限制在 `1` 至 `8`。
- 现有环境不配置该变量即可沿用默认值。
- 其他账号、渠道、计费和上传大小配置不变。

## 数据与迁移

- 不需要数据库结构迁移，也不会重写现有画布、工作台、图片任务或资产记录。
- 服务端启动后，每个账号首次发生受配额约束的写入时异步统计现有媒体大小；后续使用缓存增量。
- 已处于 `queued` 的图片任务在服务重启后重新进入队列；重启时已在上游运行的任务仍保持原有 `unknown` 防重复扣费策略。

## 升级步骤

1. 备份 `.env.deploy`、完整 `server-data` 和当前部署代码。
2. 部署 v0.12.2 发布文件，并确认 `.env.deploy` 可选配置 `IMAGE_TASK_CONCURRENCY=2`。
3. 执行 `docker compose -f docker-compose.deploy.yml up -d --build`。
4. 确认 `app`、`backend`、`gateway`、`landing` 四个容器均为 `healthy`。
5. 验证公网首页、`/api/health`、登录、画布同步、资产列表和管理后台渠道配置。
6. 浏览器强制刷新，逐页打开首页、画布、图片、视频、资产和管理后台，确认懒加载块可正常获取。

## 回滚

- 代码回滚：恢复部署前代码备份，并重建 `app`、`backend` 和 `gateway`。
- 数据回滚：本版本没有主动数据迁移；通常无需恢复数据。如写入验证发现异常，可停服务后恢复部署前 `server-data` 快照。
- 配置回滚：删除 `IMAGE_TASK_CONCURRENCY` 或恢复原 `.env.deploy` 后重建后端。
- 前端回滚不会影响 SQLite、账号媒体或现有任务记录。

## 验证标准

- 根目录 `npm run check` 全量通过：服务端 8 项、Agent 14 项、前端 TypeScript 和 Vite 生产构建。
- `VERSION`、变更日志、发布说明、Git tag 与 GitHub Release 均为 `0.12.2`，tag 指向实际发布提交。
- 同账号并发上传不会突破存储上限，批量图片任务同时运行数不超过配置值。
- 不存在或能力不匹配的默认模型返回 400，原设置保持不变。
- 生产容器全部健康，公网首页和 `/api/health` 返回 200。

## 已知事项

- 路由拆分后入口 JS 已下降，但共享依赖块仍大于 Vite 默认 500 kB 告警阈值，后续需按依赖边界继续优化。
- 存储配额首次异步统计包含现有元数据文件，运行期缓存主要按媒体与文本资产的有效载荷增减；进程重启会重新测量实际目录。
- 旧版浏览器迁移、并发额度边界和各懒加载页面仍保留在“待测试”清单，需生产人工回归确认。
- 本次上线不执行付费图片或 Grok Video 生成；付费渠道验证需单独授权。

## 生产部署记录

- 2026-08-04 已从发布源码提交 `d36af20` 生成精简生产包并部署到 `/opt/infinite-canvas`。
- 发布包：`/opt/infinite-canvas-v0.12.2-d36af20.tar.gz`，SHA-256 `0def7ae0cbf5d4835fad46fe16584ac5160cb243e62d573ae858c591acc5b182`。
- 部署前代码与完整数据备份：`/opt/infinite-canvas-backups/v0.12.2-20260804-114314/code-pre.tar.gz`、`server-data-pre`。
- 历史部署备份与旧数据快照已移至 `/opt/infinite-canvas-archive/20260804-pre-v0122-cleanup`；散落备份源码和 macOS 伴生文件已打包为 `stale-files.tar.gz` 后从部署目录清除，可从归档恢复。
- 前端镜像：`sha256:970e68e4a12b580d2343276b8d69d64eb5708f61fabdc266ff26e6c4d694f80c`；后端镜像：`sha256:442f29b78cf88166a5e26dd0300e9dad718e77e6b0b63a29f6fb33c7c891432e`。
- `server/index.mjs`、`server/task-queue.mjs`、`web/src/router.tsx` 与发布包内对应文件 SHA-256 完全一致。
- `app`、`backend`、`gateway`、`landing` 四个容器均为 `healthy`；公网首页和 `/api/health` 返回 200，线上主资源包含 `0.12.2` 与独立画布项目块。
- 浏览器实测登录页完整渲染，深层画布路由未登录时正确回到登录页，控制台无 warning/error。
- 本次未调用任何付费图片或视频生成接口，付费渠道行为仍按“待测试”清单保留。
