# HowCanvas v0.12.11

图片后台队列允许最多 10 个同时执行的任务，生产配置从 4 提高到 10。队列为空时前 10 项无需等待本地执行槽，第 11 项起排队。模型推理仍由外部 API 完成，视频上游生成独立提交；视频原任务查询、取回及本地文件校验仍共用 2 个执行槽。

## 升级前备份

在 `/opt/hoosland-archive/canvas-history-v0.12.11-20260912/` 保存旧受管源码、源码清单、当前环境配置、后端镜像引用和一致性数据备份。环境文件含密钥，只保存在服务器受限目录，不进入 Git 或公开发布附件。检查任务排空后再切换，账单与用户媒体始终保留。

## 配置变化

- 生产设置 `IMAGE_TASK_CONCURRENCY=10`。代码上限从 8 提高到 10；仅填写环境变量而仍运行旧代码不会获得 10 个执行槽。
- 未配置的新部署仍默认 2，允许范围为 1 至 10；启动日志显示实际并发值。
- 视频取回与校验并发保持 2，视频下载时限沿用 v0.12.10；所有渠道单价、余额和用户权限不变。

## 数据迁移

没有数据结构迁移，不初始化或恢复旧余额，不重建任务，不重复提交付费生成。

## 升级步骤

1. 核对 Git 主分支、生产提交与源码清单；从 v0.12.11 已提交树导出源码和清单。
2. 复用 v0.12.10 的 Node 依赖镜像，在不带生产环境变量、数据卷的临时容器中仅放入已提交的后端变更文件，封装为新后端镜像。不运行编译、依赖安装、应用构建或测试。
3. 前端没有代码变更，继续使用 v0.12.10 的前端产物；页面显示的构建版本仍可能是 v0.12.10。本次发布版本指后端与源码版本，不伪造前端产物版本。
4. 确认生成与视频取回已排空，备份当前数据和环境；设置图片并发为 10，使用新后端镜像重建后端。保留前端、网关和独立工作台运行配置。
5. 核对启动日志、运行源码、HTTP 健康、账户及账单一致性；更新部署标记并写入归档 `deployment-receipt.json`。

## 升级后验证

核对后端环境变量和启动日志均为 10，程序代码不再截断为 8；验证运行文件与源码清单一致，公网健康检查返回 200，资金与任务记录未因升级变化。按项目约定不运行构建或自动测试，不发起生产付费压力测试。

10 人真实使用、各渠道上游配额和大文件峰值仍需人工验收。详细等待时间与限制见 [并发配置](docs/CONCURRENCY_SETTINGS.md)。

## 回滚方式

若只是上游限流或资源压力增加，任务排空后将图片并发降回 4 即可。若需回滚源码，使用归档的 v0.12.10 后端镜像与匹配源码，保持当前数据库和媒体，恢复相应部署标记。不覆盖上线后新产生的用户数据或资金记录。

## 已知事项

- 当前硬件仍为 4 核、16GB、14Mbps。没有本地 LLM 推理，但媒体缓冲、存储、哈希及视频解码仍消耗本机资源。
- 10 个本地执行槽不保证上游 10 路都立即生成，也不保证保持原耗时；遇到 429、超时或大图内存峰值应降并发并排查。
- 图片队列为全站共享，尚无用户公平队列；一个用户的批量任务也会占满槽位。同 IP 提交限流保持原配置。
- 两个慢视频下载仍可能推迟其他视频状态查询；上游已受理的生成可以继续运行。
- 公网带宽、HTTPS、预览图和视频查询/下载分离继续作为独立事项处理。
