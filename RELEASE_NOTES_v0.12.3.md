# HowCanvas v0.12.3 发布说明

发布日期：2026-08-12

## 新增

- 接入独立的 MiniMax H3 视频渠道，支持文生视频、首尾帧和多模态参考素材。

## 修复

- 将阿里云签名视频结果通过受控媒体代理保存，解决浏览器 CORS 限制。
- 为 H3 任务查询设置独立限流和自动退避，避免长任务轮询触发本地代理 429 后中断。

## 升级与回滚

- 仅涉及前端视频调用与后端 AI/媒体代理逻辑，无数据迁移。
- 回滚时恢复上一版前端镜像，并将后端镜像切回发布前的 `pre-minimax-h3-rate-limit-20260812-2335` 标签。
