# v0.11.5 升级说明

发布日期：2026-07-29

## 解决的问题

v0.11.4 已能阻止旧页面把其他账号记录重新写入服务器，但浏览器中已经污染的账户级 IndexedDB 仍会参与合并并被工作台渲染，因此 nanana 可能继续看到其他账号图片。

v0.11.5 将服务器确认结果设为工作台记录归属的最终依据。同步成功后，前端会完整校正当前账号本地日志，并移除不属于服务器确认记录的媒体任务。

## 主要变更

- 切换账号时立即清空生图工作台记录和历史抽屉，不再预先渲染旧本地缓存。
- 工作台保存后重新读取服务器确认结果，完整替换当前账号 IndexedDB 日志。
- 媒体队列只允许处理当前服务器确认记录引用的 `storageKey`。
- 服务端拒绝把其他账号已拥有的工作台媒体键上传到当前账号。
- 增加跨账号记录和媒体上传回归测试。
- 增加工作台账号隔离与同步开发文档。

## 升级步骤

1. 备份项目代码、环境文件和 `server-data`。
2. 部署 v0.11.5 源码并执行 `docker compose -f docker-compose.deploy.yml --env-file .env.deploy up -d --build`。
3. 确认 app、backend、gateway 三个容器均为 healthy。
4. 确认服务器账号记录数量及重复 ID 与可信基线一致。
5. 使用已污染的 nanana 浏览器刷新页面，确认本地记录自动恢复为服务器记录。
6. 模拟跨账号记录与媒体回写，确认记录被丢弃、媒体返回 HTTP 409。

## 回滚

- 代码回滚：恢复部署前代码归档，再重建三个容器。
- 数据回滚：恢复部署前 `server-data` 备份；如只恢复被隔离媒体，可从对应 quarantine 目录按账号还原。
- v0.11.5 不执行不可逆的数据格式迁移，回滚至 v0.11.4 不需要转换 JSON 结构。

## 验证标准

- `npm test` 全量通过。
- 线上静态资源包含版本号 `0.11.5`。
- 健康接口返回 `ok: true`，公开注册保持关闭。
- admin 24 条、nanana 3 条、跨账号重复 0。
- nanana 浏览器缓存自愈后只显示 3 条服务器记录。

## 生产部署记录

- 2026-07-29 已部署 v0.11.5，app、backend、gateway 三个容器均为 healthy。
- 发布包 SHA-256：`4721ce57d59670e18e1bb147276dd241f0b67f701a6800e0ce2a4c7feac3d033`。
- 部署前代码备份：`/opt/infinite-canvas-code-20260729-221247-pre-v0115.tar.gz`。
- 部署前完整数据备份：`/opt/infinite-canvas-server-data.backup-20260729-221247-pre-v0115`。
- 旧浏览器再次回传的 23 个跨账号媒体文件及元数据已移动到 `/opt/infinite-canvas-server-data.quarantine-v0115-20260729-221611`，未删除，可按清单恢复。
- 清理后服务器记录为 admin 24 条、nanana 3 条、重复记录 0；媒体文件为 admin 23 个、nanana 6 个、跨账号重复媒体 0。
- 线上攻击回归通过：nanana 提交混合记录时服务端丢弃 24 条 admin 记录并保持本人 3 条；复用 admin 媒体键上传返回 HTTP 409。
- 公网与本机健康接口均返回 `ok: true`，线上前端静态资源包含版本号 `0.11.5`。

## 已知后续事项

- 首次打开 v0.11.5 时需要刷新仍停留在旧资源的页面；同步完成后，服务器确认结果会自动校正该账号的 IndexedDB。
- 主机尚未配置 HTTPS/443，后续仍需补齐 TLS 与 `COOKIE_SECURE` 上线验证。
- GitHub Pages 尚未启用，部分发布工作流仍指向上游 GHCR，相关检查可能因仓库权限失败。
