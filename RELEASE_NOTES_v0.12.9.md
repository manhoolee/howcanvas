# HowCanvas v0.12.9

视频创建只预扣一次，进度查询与取回不重复收费。新增按秒计价能力：按 15 秒预扣，服务器保存并核验视频后按实际时长确认费用、一次退回差额。图片接入统一账务及任务去重，用户可从账号菜单进入积分明细。

## 配置变化

- 现有点/条价格和模型覆盖价保持原值、原单位。管理员明确填写每秒单价并选择点/秒后，该模型才启用 15 秒预扣。不要把原来的点/条数值直接当成点/秒。
- 后端镜像包含 ffprobe 和 ffmpeg；非 Docker 环境使用 `FFPROBE_PATH`、`FFMPEG_PATH` 指定工具。
- 任务保存不可变渠道版本；历史密钥保存在受限的 `channel-credentials.json`，与 `channels.json` 一起保护和备份，不能进入 Git 或公开日志。
- 前后端一起更新。旧页面缺少视频请求标识时会被拒绝，用户需刷新页面后重新提交。

## 升级前备份

生产归档目录：`/opt/hoosland-archive/canvas-history-v0.12.9-20260912/`。保留升级前源码与清单、环境配置、SQLite 一致性快照、账户及渠道资料、完整媒体备份和旧镜像记录。目录包含私有资料，限制为服务器管理员访问。

旧镜像保留为 `infinite-canvas-app:v0.12.9-before`、`infinite-canvas-backend:v0.12.9-before`。发布源码包、完整校验清单与上线回执也放在此目录；公开发布只能附带不含用户及密钥数据的源码和回执。

## 数据迁移

SQLite 账户表成为余额与消费统计的权威来源，资金分录和账单事件事务入账。首次启动只为尚未存在的账户导入旧可用余额，不重复扣余额。旧缓存中明确 pending 且未退款的金额转为预扣中，从累计已消费中扣除，实际可用余额保持不变。

先在生产数据库副本上执行迁移并逐账号对账，再短暂停写、备份、切换。检查全部账户的可用余额、预扣、累计消费、账单数量、数据库完整性及历史任务状态。旧用户缓存最多保留 200 笔账单，导入台账不代表补齐所有更早历史，完整历史审计仍需结合日志与归档。

发布前发现一笔旧图片任务结果不明，对应 10 点预扣；保留待核实，不能凭时间久远自动确认或退款。历史视频重复扣费不在本次发布中自动补偿。

## 升级步骤

1. 核对主分支、线上提交和旧清单，发现源码差异先保存并合并。
2. 将本版改动提交并标记 v0.12.9，从提交导出完整源码，使用 `scripts/release-source-manifest.mjs` 校验归档。
3. 在独立发布目录准备新 app/backend 镜像，生产继续运行旧版本；用数据副本验证迁移。
4. 确认没有运行或排队的生成任务，短暂停止 Canvas 写入，备份完整数据，再同步 Canvas 受管源码并切换两个容器。保留生产环境、用户文件和独立站点。
5. 检查现有独立网关配置，保留视觉工作台到 `172.19.0.1:13092/` 的代理；只校验并重载以刷新容器地址，不用仓库旧网关参考文件覆盖。
6. 健康和数据核验通过后写入部署提交与清单，并把脱敏上线结果附到 GitHub Release。

## 验证与上线记录

本地 14 项后端测试已通过，覆盖去重、结算事务回滚、渠道保护、图片和账号权限。浏览器验证真实 6 秒测试文件：预扣 150 点、实耗 60 点、退回 90 点；桌面和手机积分明细及账号隔离通过。此处测试不代表向生产供应商发起过付费验收请求。

发布环境为 `can.hoosland.com`。实际上线日期、最终提交、迁移核对、健康检查、运行镜像与归档位置记录在归档 `deployment-receipt.json`，并附于 [GitHub v0.12.9 Release](https://github.com/manhoolee/howcanvas/releases/tag/v0.12.9)。发布过程不额外发起付费生成。

## 回滚方式

资金迁移后不能直接启用只读 JSON 余额的旧后端。优先暂停新生成，保留新账务数据库，使用兼容新账务的修复版本。已经产生新分录时，严禁用旧数据库备份覆盖现有余额。

仅在切换期间仍阻断外部写入、确认只有期初迁移分录且没有新业务操作时，才可恢复停写备份与旧镜像。恢复前先完整保存失败版本数据，核对源码、镜像、提交与清单一致，再恢复服务。独立网关和视觉工作台不参与数据回滚。

## 已知事项

- 临时取回错误、过期原因不明、没有上游任务 ID 或实际时长超过 15 秒均保留待核实，不自动追加费用。
- 旧任务缺少渠道版本时不猜测关联；管理员依据渠道记录处理。插件和部分同步生成入口尚未全部迁入后台任务去重。
- 期间报表、CSV 导出、自动关联旧任务和超过 15 秒的商业结算规则仍待后续实现。
- 公网 HTTPS 与安全 Cookie 使用现有部署配置，本版不变更独立网关的 TLS；可达性核验结果以发布回执为准。
