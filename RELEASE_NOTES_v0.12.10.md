# HowCanvas v0.12.10

视频已经生成但取回缓慢时，原先 2 分钟下载限制容易触发重试。本版默认延长到 10 分钟，并同步调整底层 HTTP 响应头、响应体等待限制。画布和视频工作台显示排队、生成、取回、核验、等待重试或待核实；有文件大小时显示真实取回字节和百分比。

## 升级前备份

归档目录为 `/opt/hoosland-archive/canvas-history-v0.12.10-20260912/`，权限仅限管理员。保存旧源码及清单、当前环境配置、停止写入后的完整数据备份、独立网关配置和 v0.12.9 镜像。数据库包含升级后新产生的账务，必须备份当前 SQLite 数据和用户媒体。

## 配置变化

- 新增 `VIDEO_DOWNLOAD_TIMEOUT_MS`，默认 `600000` 毫秒。该限制覆盖单次下载的跳转、响应头和响应体；底层 HTTP 等待同步采用此值，连接建立上限仍为 60 秒。
- 浏览器取回媒体等待延长到 10 分钟；服务器管理的视频任务前台轮询窗口延长至约 60 分钟。窗口结束仍保留原任务，服务端继续处理。
- 台账增加交付阶段、字节进度、下载开始/完成/失败和重试时间记录，不保存签名链接或凭据。

## 数据迁移

本版没有数据结构迁移，不重新导入账户，不更改单价、计费单位或历史扣费。保持现有 SQLite 账户、分录、任务及渠道版本。发布前后核对账户和资金分录；严禁运行 v0.12.9 的旧余额初始化或恢复旧数据库。

## 升级步骤

1. 核对 Git 主分支、生产提交及源码清单；仅使用 v0.12.10 已提交源码发布，校验归档和生产 Canvas 文件。
2. 在归档目录准备 app/backend 发布镜像，旧服务继续运行。确认没有排队或运行中的生成，以及正在取回的视频，再短暂停止 Canvas 写入并做一致性备份。
3. 复制 Canvas 受管源码，保留生产环境配置、数据库、媒体及独立站点，切换两个服务到 v0.12.10。
4. 保留独立网关的视觉工作台路由 `http://172.19.0.1:13092/`，只校验并重载网关，不覆盖线上配置。
5. 核对健康、运行源码、前端版本、实际超时值和账务完整性，写入 `.deployed-commit`、`.deployed-manifest.json` 及上线回执。

## 升级后验证

本地 12 项后端测试通过，覆盖慢速响应体超时、延长后完成、字节进度、文件大小限制、跳转安全及账务回归。类型检查与画布/视频工作台状态验证通过；状态验证使用本地模拟任务，没有发起生产付费生成。

生产环境为 `can.hoosland.com`。上线时间、提交、备份目录、镜像、源码校验、账户和分录核对及健康结果记入归档 `deployment-receipt.json`，脱敏回执附于 [GitHub Release](https://github.com/manhoolee/howcanvas/releases/tag/v0.12.10)。

## 回滚方式

保存失败版本的当前数据，停止 Canvas 写入，切回 v0.12.9 源码及兼容当前账务的 app/backend 镜像，继续使用当前数据目录。重载网关并核对账户、分录、任务及健康。不得用升级前数据库覆盖上线后发生的新交易；不回滚独立站点配置。

## 已知事项

- 我方尚未实现下载断点续传。已验证本次 Seedance 视频源支持非零 Range、If-Range 和稳定 ETag，不能据此认定所有渠道都支持。下载超时后仍重新取回原任务的文件。
- 下载失败不重新创建生成任务或再次预扣；原因不明保留待核实，确认费用仍依赖已保存并核验的视频。
- 超时变长可以容纳慢速传输，不会提高源站带宽；并行取回上限仍为 2。前端显示状态需要刷新至新版本。
- 现有公网 HTTPS 超时问题仍待独立处理；本次不修改网关 TLS，也不自动补偿历史重复收费。
