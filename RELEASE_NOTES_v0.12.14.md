# HowCanvas v0.12.14

“操作指南”使用与“打开画布”一致的描边按钮。指南从 5 张增加到 20 张截图，补充生成菜单、画布操作菜单、图片快捷工具及裁剪/画笔弹窗；按需展开详细截图。修正空图片节点的打开说明，截图采用相对路径，支持本地 HTML 阅读。

## 升级前备份

先核对 GitHub main、生产提交标记和源码清单。本次回滚归档为 `/opt/hoosland-archive/canvas-history-v0.12.14-20260912/`，保存旧源码、部署清单、环境配置、前端镜像和 SQLite 在线备份。账号、密钥与用户媒体不进入 Git。

## 配置变化与数据迁移

无新增配置，无数据库或用户数据迁移。更新 `/guide.html` 和 `/guide-assets/`，仅发布前端，后端沿用 v0.12.11。教程采用示例画布；提示词库截图为公开素材库的搜索结果。

## 升级步骤

1. 从 v0.12.14 已提交 Git 树生成并核验源码清单，导出归档，在独立目录生成正式前端产物。
2. 备份生产状态，以现有前端镜像封装候选镜像，保留旧资源并逐文件核验。
3. 仅替换 Canvas app 容器，检查静态资源、指南图片、后端及独立站点路由；通过后同步受管源码和部署标记。
4. 将上线提交、镜像、时间、备份和验证结果记录到 GitHub Release 与服务器发布回执。

## 升级后验证

本地确认两个按钮的高度、边框和圆角一致；20 张截图全部加载，折叠栏目可展开，390px 深色窗口无横向溢出，file:// 打开本地 HTML 时全部图片可读。已生成 A4 预览，并确认打印样式显示折叠内容。生产核验资源和受管源码哈希，检查指南、截图及应用健康；不提交收费生成。

## 回滚方式

将 `infinite-canvas-app:v0.12.14-before` 恢复为 `infinite-canvas-app:latest`，仅重新创建 app 服务。恢复旧受管源码与部署清单，将新增文件移入归档。后端与当前用户数据持续运行，不恢复旧数据库覆盖新数据。

## 已知事项

模型、单价、可见工具随账号与配置变化；截图用于说明操作。指南图片合计约 1.3 MB。新手完整操作与实际打印分页仍待用户验收，其他既有待办保持原状态。
