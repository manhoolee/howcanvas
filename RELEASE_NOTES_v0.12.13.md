# HowCanvas v0.12.13

开始页“打开画布”旁新增“操作指南”，打开简明 HTML 图文教程。包含开始页、画布库、图片参数、参考连线及视频积分 5 张当前界面截图，支持点击放大、目录跳转、深色模式、手机阅读及打印。

## 升级前备份

核对 GitHub main 与生产部署清单，备份旧受管源码、环境配置、部署标记和前端镜像。回滚资料放在 `/opt/hoosland-archive/canvas-history-v0.12.13-20260912/`，数据库通过 SQLite 在线备份保留；业务数据与密钥不进入 Git。

## 配置变化与数据迁移

无新增配置，无数据库迁移。新增公开静态页面 `/guide.html` 和 `/guide-assets/` 截图；页面不包含账号资料、真实项目内容或密钥。截图来自当前组件及示例数据，报价仅供说明。后端继续使用现有 v0.12.11 镜像。

## 升级步骤

1. 从已提交的 v0.12.13 Git 树生成源码清单并导出归档，在独立目录生成正式前端产物。
2. 备份生产配置与镜像，封装候选前端并核验静态资源；保留旧静态资源供已打开页面使用。
3. 仅替换 Canvas app 容器，核验指南、图片、首页、后端和独立站点路由后同步受管源码与部署清单。
4. GitHub Release 和服务器发布回执记录提交、镜像、备份位置及线上验证结果。

## 升级后验证

本地已验证 5 张截图正常加载、图片新标签页放大、8 个目录锚点、常见问题展开及 390px 深色窗口无横向溢出，并生成 A4 打印预览。开始页入口用当前页面组件验证。上线逐文件核对静态资源和源码哈希，验证指南与图片经公网可读；不触发收费生成。

## 回滚方式

将 `infinite-canvas-app:v0.12.13-before` 重新标记为 `infinite-canvas-app:latest`，仅重新创建 app 服务；按归档恢复旧受管源码、部署标记，新增文件移入归档。后端与用户数据继续运行，不覆盖当前数据库。

## 已知事项

教程截图使用示例内容，模型、单价、账号权限以实际配置为准。新手按图完成创作及不同打印机的分页效果仍待用户验收。已有 HTTPS、媒体带宽等待办保持原状态。
