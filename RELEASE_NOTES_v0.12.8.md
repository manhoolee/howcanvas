# HowCanvas v0.12.8

本次更新 GPT Image 2.5 参数菜单、多选对齐、连线删除与复制，同时修复生成结果传输慢和长任务提前超时。GitHub 主分支补齐已发布的 v0.12.7，并以同一提交发布本地和服务器源码。

## 行为变化

- GPT Image 2.5 前端只显示 Flare、Sunburst；分辨率选择自动映射 API，使用精确 `size` 像素值，质量独立选择。具体支持范围与实测限制见 [参数验证记录](docs/GPT_IMAGE_25_COMPATIBILITY.md)。
- 框选后右键支持左、右、顶、底、水平居中与垂直居中对齐，组内元素一起移动，支持撤销和保存。
- 连线中部删除按钮支持点击与键盘操作；右键复制节点继承输入、输出连接并保留原连接。
- T8 Flare、Sunburst 后台请求使用 URL 返回；图床原图由服务器下载并保存，再交给前端缓存与显示。
- AI 响应头、响应体和总请求等待统一遵循 20 分钟配置，修复底层默认 5 分钟提前超时。连接等待 60 秒，生成完成后的服务器取图允许 120 秒。

## 升级前备份

本次发布备份位置为 `/opt/hoosland-archive/canvas-history-v0.12.8-20260910/`，保留旧源码、环境配置、在线 SQLite 备份和旧镜像记录。旧 app/backend 镜像添加 `v0.12.8-before` 标签。目录含私有运行资料，不可上传 GitHub。

此前各项修复备份仍保留在 `/opt/infinite-canvas/backups/`，本次不删除。后续新增回滚资料统一存放在生产目录之外。

## 配置与数据迁移

无数据库结构迁移，无新增必填密钥。生产继续使用 `AI_UPSTREAM_TIMEOUT_MS=1200000`，`IMAGE_DOWNLOAD_TIMEOUT_MS=120000`；新增 `undici` 依赖固定在锁文件中。`VERSION` 是界面版本来源，子包的内部版本字段保持原有约定。

## 升级步骤

1. 获取最新 Git 主分支，核对服务器 `.deployed-commit`。从 v0.12.8 起，先用 `node scripts/release-source-manifest.mjs verify /opt/infinite-canvas /opt/infinite-canvas/.deployed-manifest.json canvas` 检查生产源码；不一致时先保存并合并改动。
2. 从已提交的 Git 树导出源码包并生成外部 SHA-256 清单；完整源码保存于上述归档目录的 `source/`，Canvas 运行目录仅同步 Canvas 文件。仓库内旧主页、Insight、课程目录及 `nginx.deploy.conf` 不覆盖独立线上站点和网关。
3. 保留 `.env.deploy`、`server-data/`、用户图片和运行依赖，构建前端以更新显示版本；后端仅在需要时重建，重启前检查没有排队或运行的生图任务。
4. 检查网关视觉工作台代理仍指向 `172.19.0.1:13092/`，容器更新后校验并重载网关配置以刷新后端地址。
5. 逐文件校验完整归档和生产 Canvas 源码，通过后写入 `.deployed-commit` 与 `.deployed-manifest.json`，核对 GitHub 主分支及 `v0.12.8` 标签。

## 验证与上线记录

发布环境：`can.hoosland.com`，2026-09-10；本地规范开发目录：`F:/hoosland/can/source`。

- 参数测试、六种对齐、连线删除/复制及撤销保存已完成针对性自动化验证，记录保留在测试文件及待测试清单。
- 超时测试覆盖模拟 610 秒成功、1201 秒超时和取消，生产 Node 22 下通过；无需额外付费生图。
- 真实 16:24 任务：上游生图 49 秒，16:25:19 返回图片链接，16:25:23 服务器保存，16:25:25 前端显示；总交付约 56 秒。详见 [交付时序与超时记录](docs/IMAGE_DELIVERY_TIMING.md)。
- 发布后检查画布首页、API 健康、独立视觉工作台页面和就绪接口，验证新静态资源包含 v0.12.8。最终部署回执与源码清单保存在发布归档，并随 GitHub Release 提供。

## 回滚

使用备份的源码和版本标记恢复 Canvas 文件，将 app/backend 镜像恢复为 `v0.12.8-before` 并更新相应服务，重新校验网关和健康接口。没有数据迁移，常规回滚无需覆盖用户数据库或图片。恢复的源码清单和提交号必须对应回滚版本，不能只改 VERSION。

## 已知事项

- 我方 20 分钟等待不能延长供应商自己的超时限制；超过 20 分钟仍会失败，手动取消保持有效。
- 部分供应商质量值可能被接受但归一化，准确尺寸和参数实测结论以参数验证文档为准。
- 弱网、浏览器冻结、跨浏览器恢复等长期场景仍保留在待测试清单；本次不宣称全部人工验收完成。
- HTTPS 的既有可达性问题仍待独立处理，本次在线验证使用 HTTP。
- 三方同步范围是受 Git 管理的源码与发布版本；环境密钥、数据库、用户媒体、历史备份、依赖和构建缓存不进入 GitHub。
