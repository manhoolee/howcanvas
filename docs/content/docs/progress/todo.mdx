---
title: TODO
description: 当前项目后续值得处理的事项
---

# TODO

本文档用来记录当前项目后续比较值得处理的事项。

- 视频断点续传：已验证本次 Seedance 视频源支持非零 Range、If-Range 和稳定 ETag，但我方下载程序暂不续传；后续实现部分文件保留、验证器匹配、忽略 Range 时安全重下及链接续期处理，并逐渠道验证。当前优先采用 10 分钟取回超时。

- 同模型多渠道调度：按[调研与升级方案](../../../MULTI_CHANNEL_ROUTING_UPGRADE.md)先盘点等价渠道与独立故障域，再实现逻辑模型池、持久任务与尝试、按渠道限流、加权分配、安全切换和一次结算；结果不明的请求不得盲目重新生成。先完成渠道容错，再独立推进多实例高可用、共享状态和发布排空。当前仅完成方案文档，尚未实现或部署。

- 按秒计费后续：核心预扣、实长结算、退差额与渠道保护纳入 v0.12.9；开启按秒价格前仍需逐模型确定点/秒价格、核实渠道能力。发布执行余额迁移核对，结果见[升级说明](../../../../RELEASE_NOTES_v0.12.9.md)。继续完善期间分录报表/CSV 导出、旧任务自动关联和超过 15 秒的商业结算策略，当前超限保留待核实。

- 历史计费对账：按线上日志核实并处理旧视频轮询产生的重复扣费，本版发布不自动补偿。旧账单缺少任务 ID，不自动补造上游任务关联。渠道已接收但提交响应丢失、没有上游任务 ID 的预扣记录，需要按渠道日志对账后处理。

- 图片交付后续：按渠道实际额度设置限额和用户公平队列；补齐服务端任务与画布节点的持久关联及预览图。上游首字节与响应体时序已加入任务记录，T8 GPT Image 2.5 的 URL 原图交付进入待测试。v0.12.11 的 10 槽已完成四模型 12 图、客户端取图、浏览器恢复和计费幂等实测；持续多用户、大文件、异常退款及更高并发仍需验证。14Mbps 出口实测已接近饱和，后续扩容需同时评估交付带宽，详见[并发配置](../../../CONCURRENCY_SETTINGS.md)。

- 视频并发后续：原任务查询与下载/校验目前共享 2 个执行槽，两个慢下载会推迟其他视频状态查询。需要扩容时先分离短查询和媒体处理的限流，保留原任务绑定及幂等结算；本次视频执行槽保持 2，未验证上游 10 路实际生成能力。

- 在已登录生产环境完成画布 Agent 的消息级固定选区、任务执行契约、选中图标、完整文字流和实际画布修改回归；服务器方案三不依赖 `@basketikun/canvas-agent`。
- 本地 Agent 接入后续：按[本地 Agent 接入规划](/docs/progress/local-agent-integration-plan)把 Claude Code CLI Adapter 升级为 Claude Agent SDK Adapter，并完善工具队列体验。
- 生产 HTTPS：为公网网关配置域名与 TLS，启用 `COOKIE_SECURE=true`，稳定后再加 HSTS，避免登录与 AI 流量经公网明文传输。
- 第三方插件隔离：在当前显式信任确认之上，评估 iframe/Worker 沙箱、权限清单和可审计的能力桥接，降低插件与主页同权运行的剩余风险。
- Agent 流式鉴权：将 EventSource 连接迁移为可携带请求头的 fetch 流或 WebSocket，彻底移除 URL 查询参数中的本地 Agent 令牌。
