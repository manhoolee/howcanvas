# 画布运行中心验收记录

## 本地验证

- 使用隔离工作目录、测试数据库与本地模拟上游，测试样本没有部署到生产。
- 严格成功率、部分成功、未知、取消、无样本、加权总计、24小时半开区间、按任务输出去重、CSV公式保护已覆盖。
- 独立分析库回填、事务重放、重启幂等、用户删除后保留统计、图片任务与账单合并、积分可用/冻结对账已覆盖。
- 新集成测试通过管理员与普通用户鉴权、两个标签页去重、理由必填、图片并发范围校验、暂停/恢复渠道、成功/失败生成和文件保存、用户/模型/时间筛选、导出、退出会话、审计和重启恢复。
- 既有服务端回归、TypeScript检查和前端构建纳入发布门禁。
- 浏览器以真实界面检查11个栏目、图表渲染、任务追踪和深浅主题/390px布局。截图在本地工作目录 `output/playwright/`，合成数据仅供布局与口径验收。

## 生产验证

发布执行回执写入 `/opt/hoosland-archive/canvas-history-v0.12.15-20260913/acceptance.json`，其中保存版本/提交、源码校验、容器与公开路由、生产聚合对账和备份恢复结果。最终核验以该回执及发布后的本页记录为依据。

## 持续观察

历史在线和资源没有旧采集值，上线后开始真实积累；七日稳定性未预先声明通过。供应商成本、流式Token未计量和外部直连不冒充已接入。页面明确覆盖与未知，便于后续核实。

## 上线验收结果 · 2026-09-13

v0.12.15 已部署到 `http://can.hoosland.com/admin/monitor`。后端、前端和 Prometheus 均健康；网关、主页、Insight 和独立视觉工作台保持健康，网关配置未改变。

- 本地27项服务端测试全部通过，TypeScript与前端构建通过；11个栏目无浏览器脚本错误，390px无页面横向溢出。
- 501项画布源码与发布清单一致；78项公网静态文件与构建产物哈希一致，运行期 `config.js` 按现有入口脚本生成并单独核验。
- 通过实际网关验证：未登录401、普通用户403；管理员可以查询、筛选和导出五类数据，实时SSE连续两帧间隔约5秒。30天整页统计三次约63—70ms，仅代表本轮当前数据量实测。
- 803个图片任务逐项核验；3个具备完整交付证据的视频任务纳入统计。合计806个真实任务、652个已记录成品；650成功、99失败、26取消、31未知。全历史严格成功率为650/749＝86.78%，与任何特定30天窗口不可混用。
- 早期账单以任务内 receiptId 关联，迁移账单不虚增生成任务。30个旧成功记录缺少成品保存证据，改为统计层待核实，业务源记录未修改。
- 发布前后803个图片任务、663笔账单、50条积分分录和账户可用/冻结合计一致；所有账户积分对账差异为0。
- 私网Prometheus采集 `up=1`；宿主机采样正常。数据库与分析库备份、独立副本完整性恢复检查通过；每日04:20备份定时器已启用，采样每15秒执行。
- 发布前4.5GB运行数据、副本数据库、源码、配置和旧镜像均已保留。回滚与验收材料位于 `/opt/hoosland-archive/canvas-history-v0.12.15-20260913`，日备份在 `/opt/hoosland-archive/canvas-monitor-backups`。

未将历史未采集的资源/在线数据、供应商成本或七日持续稳定性声明为已完成验证；这些边界已在页面和运行说明中标明。
