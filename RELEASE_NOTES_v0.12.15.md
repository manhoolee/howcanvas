# v0.12.15 · 画布运行中心

新增管理员监控与分析入口 `/admin/monitor`，用趋势、堆叠柱图、环图、排行和热力图分析负荷、在线、任务、成功率、用户、模型、成品、积分和留存。原账户、价格及渠道配置入口继续使用现有后台。详细口径见 [运行说明](docs/ADMIN_MONITOR.md)，总体规划见 [行动方案](docs/ADMIN_MONITOR_PLAN.md)。

## 升级前备份

生产路径 `/opt/infinite-canvas`。发布前验证 `.deployed-manifest.json` 与474项画布源码一致，基线提交 `2f80a02e5f8962a0fe7e9214d8abe1356a25c2cf`。本次回滚资料位于 `/opt/hoosland-archive/canvas-history-v0.12.15-20260913`，包括4.5GB运行数据副本、SQLite在线一致性备份、旧源码、旧静态资源和网关配置。旧镜像标记 `infinite-canvas-app:rollback-v0.12.14-20260913` 与 `infinite-canvas-backend:rollback-v0.12.14-20260913`。

## 配置与迁移

追加 `docker-compose.monitor.yml`，仅重建 Canvas 前后端并启动私网 Prometheus。后端新增事务 outbox 与设置表，初始化独立分析库并回填可验证历史。余额、账单结算、用户权限与渠道密钥保持原业务数据。分析卷 `infinite-canvas_canvas-observability`，Prometheus 卷 `infinite-canvas_canvas-prometheus`。

`OBSERVABILITY_DATA_DIR` 指定分析目录，`OBSERVABILITY_HOST_FILE` 指定只读宿主机快照，`OBSERVABILITY_ENABLED=false` 可禁用工作线程和主动采样。事务 outbox 仍保留业务变动，停用期间需观察增长，重新启用后自动追平。

## 发布步骤

1. 提交代码、更新版本与锁文件，用已提交树建立全量源码归档和哈希清单。
2. 校验线上基线；独立备份源代码、数据库、运行媒体与镜像。
3. 构建经过本地回归的前端产物和后端镜像；上线只覆盖清单内画布源码，排除数据、密钥及独立站点/网关配置。
4. 合并部署配置启动 app、backend、prometheus。安装 `canvas-host-monitor` 采样和 `canvas-backup` 备份 systemd service/timer，执行一次备份及恢复验证。
5. 检查容器、私网指标、后台权限、历史统计对账、公开页面静态资源、独立视觉工作台路由和源码哈希。记录上线验收回执。

## 已执行验收

本地服务端既有20项回归通过；新增严格成功率、空样本、24小时边界、输出唯一性、CSV安全、历史回填、分录对账、重播/重启与权限/控制集成测试通过。模拟上游真实走任务受理、生成、文件保存和失败退款路径，未调用付费模型。8个并发统计查询在本地样本下最大27ms；该结果不是生产极限容量声明。TypeScript 和前端构建通过。浏览器覆盖11个栏目、桌面/390px与深浅主题，检查图表和表格筛选、热力图、任务追踪。

生产最终检查记录于 `docs/ADMIN_MONITOR_ACCEPTANCE.md`。上线后采样与长期观察继续累积，不能把不存在的历史资源或七日观察当成已验证数据。

## 回滚

先保留上线后的当前数据，停止新增监控采样/备份定时器。将旧前后端镜像恢复为部署标签，使用原部署配置只启动 app/backend；停止 Prometheus，保留新分析卷供排查。恢复旧源码及对应清单，不用旧数据库覆盖上线后的任务与积分。新增 outbox/设置表可留存，旧服务忽略它们。只有数据库损坏且明确完成数据恢复评估时才考虑恢复数据库，避免丢失上线后的真实交易。

独立网关与视觉工作台配置无需回滚；本次不修改它们。

## 已知数据边界

- 历史在线、资源、前端错误、访问行为没有采集，曲线自本次上线开始；历史图片/视频/账单按保留证据回填。
- 部分旧批量任务缺少预期数量、旧文本缺少完整响应结果，因此相应比率标记覆盖不足。
- 供应商真实成本/配额与外部直连结果未接入；流式 Token 未返回计量时保持未知。
- 导出最多10万任务；更大规模按规划启用预聚合和异步导出。告警当前在后台查看，无外部消息接收人配置。
- 日备份覆盖数据库及账户渠道配置；媒体已有发布前全量副本，日常全媒体异地备份是独立运维容量事项。

历史回填修正：以图片任务中的 receiptId 关联早期账单，迁移生成的账单编号不视为生成任务编号；无法关联的旧账单保留对账。旧成功状态没有媒体保存证据时记为未知，本次生产发现的30条此类记录已纳入该规则。

## 生产发布回执

v0.12.15 已于2026-09-13上线。27项服务端测试、TypeScript与构建通过；501项源码和78项公网静态资源哈希通过。公网权限401/403、实时SSE约5秒、30天统计约63—70ms、账户对账差异0，备份及独立恢复校验通过。三个Canvas相关容器健康，独立网站路由正常。完整结果见[上线验收](docs/ADMIN_MONITOR_ACCEPTANCE.md)。
