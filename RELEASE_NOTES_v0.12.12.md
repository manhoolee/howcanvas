# HowCanvas v0.12.12

画布生成输入区从 600px 加宽至 840px，积分显示在生成按钮左侧。切换模型、图片张数或视频时长后报价即时更新；窄窗口自动换行，保持菜单和生成按钮可见。

图片按现有模型单价乘以张数显示，模型覆盖价优先于默认价，尺寸、画质及风格沿用现有计费规则。按秒视频区分所选时长的预计消耗与统一 15 秒预扣，自动时长只显示预扣并提示按实结算。管理员显示零积分，价格加载完成前不显示默认报价。

## 升级前备份

先核对 GitHub main、生产 `.deployed-commit` 和 `.deployed-manifest.json`。本次回滚资料保存到服务器受限目录 `/opt/hoosland-archive/canvas-history-v0.12.12-20260912/`，包含旧受管源码、部署清单、环境配置、前端镜像引用和数据库在线备份。密钥和业务数据不进入 Git 或发布附件。

## 配置变化与数据迁移

无新增环境变量，无数据库或用户数据迁移。图片执行槽保持 10，视频取回池保持 2；所有价格、用户余额、扣费接口和后端服务沿用当前配置。后端运行镜像仍为 v0.12.11，前端产物升级为 v0.12.12。

## 升级步骤

1. 从已提交的 v0.12.12 Git 树生成完整源码清单，在独立目录导出并核对归档字节。
2. 为本次上线生成正式 Vite 前端产物，以现有前端运行镜像为基础封装新镜像，不挂载生产数据或读取生产密钥。保留上一版静态资源，供已打开的旧页面加载。
3. 备份生产受管源码、部署标记、运行配置、旧镜像，并执行 SQLite 在线备份；后端及业务任务继续运行。
4. 核对候选前端资源后，仅替换画布 app 容器；通过 HTTP 健康与版本检查后更新受管源码、部署标记及清单。保留服务器已有的独立项目文档。
5. 核验主站、画布、后端与独立视觉工作台路由，记录发布时间、镜像、备份和验证结果到归档 `deployment-receipt.json` 及 GitHub Release。

## 升级后验证

已在本地模拟账号和价格下完成模型与图片张数切换、视频 6 秒/10 秒及自动时长、模型覆盖价与默认价、零价、六位小数、管理员免扣及价格加载态检查；浅色、深色和 390px 窄窗口布局通过。

上线核对正式产物版本、积分文案与 840px 布局，检查 HTTP 首页、前端资源、后端健康及独立站点路由。发布过程不发起收费生成。具体线上验证结果补充在 GitHub Release 和服务器发布回执中。

## 回滚方式

将 `infinite-canvas-app:v0.12.12-before` 恢复为 `infinite-canvas-app:latest`，在生产目录执行 `docker compose -p infinite-canvas -f docker-compose.deploy.yml up -d --no-build --no-deps --wait app`。如需回退源码，恢复归档的旧受管源码和部署清单；本版新增文件按清单移入归档。后端和当前用户数据保持运行状态，不用旧数据库覆盖上线后的交易、任务或媒体。

## 已知事项

- 显示为当前已加载的报价，最终权限、额度及结算以服务器为准；显示价格不会创建或扣费。
- 按秒视频最终消费取决于服务端测量的实际时长，预计消耗不等于预扣金额。
- 生产真实生成/停止、不同画布缩放及持续多用户使用仍需实际场景验收；本次不重复付费验证生成链路。
- HTTPS、媒体带宽、公平队列及视频查询与下载分池沿用现有待办。
