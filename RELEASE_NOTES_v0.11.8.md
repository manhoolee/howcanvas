# v0.11.8 升级说明

发布日期：2026-08-01

## 解决的问题

视频任务由渠道成功生成并扣费后，如果画布页面在查询阶段断线、刷新或遇到网关短暂中断，旧版只保存通用错误，没有保留上游任务 ID。用户只能看到「任务查询失败」，而重试会创建新任务并可能再次扣费。

v0.11.8 将异步生成任务 ID 作为画布节点持久化数据，并在节点错误态和左侧画布元素列表提供不扣费的结果取回入口。

## 主要变更

- 新增 `doubao-seedance-2-0-260128` 视频模型调用与 T8 Seedance v3 任务路由适配。
- 视频任务创建后立即保存任务 ID、提供方和模型；页面重载后可继续查询。
- 错误视频节点增加「取回结果」，只查询已有任务，不创建新任务。
- 左侧「画布元素」的每一行增加任务状态刷新；无任务 ID 的手工元素显示为不可查询。
- 图片任务通过服务器持久化任务接口单次刷新；视频任务通过提供方任务 ID 单次刷新。
- 成功任务可重新下载媒体并回填原节点，再同步到账号私有画布存储。
- 保留 v0.11.7 的 SQLite/WAL 服务器主数据、单设备会话、图片后台任务与增量同步能力。
- 修复分阶段部署时的迁移窗口：仅当旧 JSON 文档时间严格晚于 SQLite 快照时，才将其补入数据库。

## 数据与计费边界

- 刷新和取回操作不调用新任务创建接口，不扣除平台额度。
- 已经成功的任务可重新下载，用于恢复渠道有结果但画布本地媒体缺失的情况。
- 旧节点如果未保留任务 ID，需从渠道结果详情手工补入一次。
- 生成结果下载后按账号隔离保存，不依赖渠道临时 URL 长期存活。

## 升级步骤

1. 备份部署代码、`.env.deploy` 和完整 `server-data`。
2. 部署 v0.11.8 源码。
3. 执行 `docker compose -f docker-compose.deploy.yml up -d --build`。
4. 确认 app、backend、gateway 三个容器均为 `healthy`。
5. 验证 `/api/health`、登录、画布同步、图片任务查询和 Seedance 任务查询。
6. 浏览器强制刷新，确认左侧画布元素显示刷新入口。

## 回滚

- 代码回滚：恢复部署前代码备份，再重建 app、backend 和 gateway。
- 数据回滚：恢复部署前 `server-data` 完整备份。
- v0.11.8 仅向画布节点元数据增加可选任务字段，v0.11.7 前端会忽略这些字段。
- 若只回滚前端，服务器 SQLite 与媒体数据不需要转换。

## 验证标准

- `npm test` 全量通过。
- `VERSION` 和线上静态资源均为 `0.11.8`。
- GitHub 私有仓库默认 `main`、发布分支和 `v0.11.8` 标签指向同一提交。
- app、backend、gateway 三个容器均为 `healthy`，公网首页和 `/api/health` 返回 200。
- 已成功 Seedance 任务可通过原任务 ID 返回结果，且画布私有媒体文件存在。

## 生产部署记录

- 2026-08-01 已从本地发布提交 `a3722cc7b655a32819f66dacb1687c59da087e2a` 生成干净发布包并部署。
- 最终发布包：`/opt/infinite-canvas-v0.11.8-a3722cc.tar.gz`，SHA-256 `2180ac4794f6acb24db0c55d811bb2135d304827660c273e0fcbeb2c252a42dc`。
- 部署前代码备份：`/opt/infinite-canvas-code-20260801-012150-pre-v0118.tar.gz`。
- 部署前完整数据备份：`/opt/infinite-canvas-server-data.backup-20260801-012150-pre-v0118`。
- 前端容器镜像：`sha256:127a766b63546bcaeb620b84dd9a42370cb870d7425a862ecf94abfb73d42f7e`。
- 后端容器镜像：`sha256:bae6bd9d5905f3aab4f5a1968c0303123102c66205249fbe768facfe3b4b6bf4`。
- 线上主包：`assets/index-DA9P42li.js`，SHA-256 `5520ed8ce252d61329982ed52ce0763debf3a90691e05663bf37fcaa02e4ee44`。
- app、backend、gateway 均为 `healthy`，公网首页与 `/api/health` 均返回 200。
- 静态资源已验证包含 `0.11.8`、「刷新任务状态」和「取回结果」。
- 发布中发现 SQLite 中该账号仅有 2 个旧项目，而旧 JSON 已更新为 3 个项目；迁移修复上线后已将较新 JSON 补入 SQLite，目标项目和 Seedance 节点均已恢复。
- Seedance 任务 `2083227386098626561` 实测查询返回 HTTP 200 / `succeeded`，私有视频文件存在，大小 2,875,351 字节。

## 已知事项

- 正在运行旧静态资源的浏览器需执行强制刷新。
- 没有任务 ID 的历史元素无法自动反查渠道任务。
- 生成提供方保留期过后，只能使用已同步到画布私有存储的媒体文件。
