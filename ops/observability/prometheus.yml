global:
  scrape_interval: 15s
  evaluation_interval: 15s
scrape_configs:
  - job_name: canvas
    metrics_path: /internal/metrics
    static_configs:
      - targets: ['backend:8787']
rule_files:
  - /etc/prometheus/canvas-alerts.yml
