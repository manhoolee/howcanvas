# HowCanvas v0.12.7

本次修复图片已经生成，但本地下载中断、恢复不及时或重复上传导致的交付等待。服务器继续保存原图，前端恢复只取回原任务，不重新生成或扣费。

## 行为变化

- 图片 GET 最多尝试三次，每次允许 45 秒无数据进度、最多 5 分钟总时长；收到新数据会重置无进度计时。401 等不可重试错误立即提示。
- 页面可见、网络恢复、SSE 重连及可见页面的定期恢复会继续取回未交付任务；不重新构造参考图请求。
- 服务器结果返回当前账户媒体索引；本地校验与缓存后登记为已同步，避免把原图上传回服务器。
- 画布媒体同步最多并发三项，保存过程中产生的修改进入后续保存。
- clientAckAt 表示客户端已完成缓存，clientRenderedAt 单独表示后续页面绘制回执。旧客户端的无 stage 回执仍可接受。

## 升级前备份

在 `/opt/hoosland-archive/canvas-history-<timestamp>/` 保存旧源码、环境配置、版本标记、数据文件归档与 SQLite 在线备份，并给当前 app/backend 镜像添加回滚标签。不要在画布生产目录内存放历史备份。

## 配置与数据

没有新增必填配置，没有数据库结构迁移。新增回执字段保存在既有任务 JSON 中。生成队列并发仍使用现有配置，默认值 2；本次没有扩大上游并发或改动网关。

## 升级步骤

1. 核对当前服务器文件和发布基线，备份代码、数据与镜像。
2. 仅更新本次涉及的代码、版本和文档，保留 `.env.deploy` 与 `server-data`。
3. 构建 app/backend；确认没有 queued/running 图片任务后，再依次更新 backend 与 app。
4. 更新 `.deployed-commit`，检查健康状态与新静态资源。主页、Insight 和独立网关不参与重建。

## 验证

针对性验证覆盖：持续下载超过 20 秒、网络错误重试、401 与取消、下载去重、媒体索引复用、缓存先于绘制回执、账号切换、损坏图片拒绝、可见性恢复，以及三路同步与后续保存。后端验证结果索引与账户隔离、分阶段回执的幂等性，并执行已有安全与路由测试。

真实账号下的长耗时生图、休眠恢复和跨浏览器场景保留在待测试清单；模拟测试不替代真实上游耗时验收。

## 回滚

从对应归档恢复本次修改文件、旧 VERSION 与 `.deployed-commit`，将 app/backend 恢复为归档中记录的旧镜像并重建这两个服务。没有数据迁移，常规回滚无需恢复或覆盖生产用户数据。

## 已知事项

- 本次不保证缩短上游自身生成或全局队列等待时间。
- 浏览器被系统完全冻结期间无法执行下载；服务器继续保存结果，浏览器恢复后重新取回。
- 缓存回执失败不阻塞图片显示，可能暂时缺少交付计时。
- 最终节点引用仍依赖画布保存；服务端任务到节点的独立持久关联与预览图属于后续工作。
